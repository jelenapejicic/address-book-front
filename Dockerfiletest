# Extending image
FROM node:carbon

#RUN apt-get update
#RUN apt-get upgrade -y
#RUN apt-get -y install autoconf automake libtool nasm make pkg-config git apt-utils

# Create app directory
RUN mkdir -p /usr/src/app
WORKDIR /usr/src/app

# Versions
RUN npm -v
RUN echo dockerfiletest
RUN node -v

# Install app dependencies
COPY package.json /usr/src/app/
COPY package-lock.json /usr/src/app/
#COPY lll.sh /usr/src/app
RUN npm install
#RUN npm install eslint -g
RUN node_modules/.bin/eslint -c .eslintrc.js -f checkstyle . --ext .js,.jsx > eslint.xml

#RUN dos2unix lll.sh; chmod 755 lll.sh

#CMD [ "sh", "lll.sh" ]
CMD echo aaaaaaaaaaa > xxxx.txt
CMD cp eslint.xml /usr/src/test